const { buttons } = require('./buttons')
const { container } = require('./container')
const { transactionTable } = require('./transactionTable')
const { walletTable } = require('./walletTable')
const { forms } = require('./forms')
module.exports = {
  buttons,
  container,
  transactionTable,
  walletTable,
  forms
}
